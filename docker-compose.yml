services:
  quick-spin:
    image: armory/quick-spin:latest
    container_name: quick-spin
    ports:
      - "9000:80"
    volumes:
      - mysql-data:/var/lib/mysql
      - type: bind
        source: ~/.kube/kubeconfig.quick-spin
        target: /home/spinnaker/.kube/config

volumes:
  mysql-data:
    name: quick-spin-mysql
